# Dummy Website 🚧

A small static demo site for testing and prototyping.

## Files
- `index.html` — main page
- `css/styles.css` — basic styling
- `js/script.js` — small interactivity
- `server.js` — simple Node static file server

## Quick start
- Open `index.html` in your browser (no server required).
- Or run:

```bash
node server.js
```

then visit `http://localhost:3000`.

---

Tips: Use Live Server or similar VS Code extensions for instant reload while editing.